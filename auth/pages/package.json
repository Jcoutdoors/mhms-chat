{
  "name": "collier-auth-pages",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VIF auth Pages project (auth.mentalhealthmadesimple.life). Phase 0 proof routes preserved; Phase 3 routes not implemented. Not deployed by Phase 2.",
  "scripts": {
    "test": "node --test test/proofRoutes.test.mjs test/authRoutes.test.mjs",
    "build": "wrangler pages functions build --outdir=.pages-build",
    "dev": "wrangler pages dev public --do VERIFICATION_DO=VerificationDO@collier-verification-do"
  },
  "devDependencies": {
    "wrangler": "^4.115.0"
  }
}
